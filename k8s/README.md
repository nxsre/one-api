# Kubernetes

- **集群通用原则**（多实例、Redis、负载均衡、路由/限速）：**[docs/cluster-deployment.md](../docs/cluster-deployment.md)**
- **Kubernetes 清单与主从角色**：**[docs/kubernetes-deployment.md](../docs/kubernetes-deployment.md)**

示例清单：`example/`（部署前请编辑 `secret-config.yaml` 与 Deployment 中的镜像名）。**HTTP / HTTPS 探针**对照见 `docs/kubernetes-deployment.md` 第 4 节与 `example/deployment-*.yaml` 内注释。
