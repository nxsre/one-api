# Changelog

## 平台管理三合一改造 (2026-05-18)

### 用户管理
- 引入了全新的 `RoleSuperAdmin (50)` 角色，介于普通管理员和 Root 之间，可以管理平台的一切功能，但无法管理租户生命周期及升降其他超级管理员。
- 用户管理列表重构为服务端分页，大幅提升加载和渲染速度，同时显示用户的租户归属情况。
- 增加了针对独立用户/租户用户的范围过滤，及针对指定角色（如超级管理员、租户管理员）的快捷筛选能力。

### 模型目录
- 模型目录（Model Catalog）支持历史版本化（version, status 字段），修改及同步自动生成新版本，旧版本自动标记为过期。
- 新增 `aliapi.me` 匿名模型抓取同步功能，一键补齐所有大厂模型的计费信息与配置。
- 前端页面支持历史版本的查看，新增 `包含过期版本` 的切换开关。

### 监控指标
- 接入 Prometheus 指标采集，暴露 `/metrics` 监控端点（受鉴权保护）。
- 增加了针对 `模型计费倍率回退`、`转发请求总数`、`转发持续时间`、`客户端意外断连 (broken pipe)` 以及 `熔断断路器触发` 的监控探针。
- 新增通过配置开关启用的高基数指标监控，允许细粒度监控各 `user_id` 的额度消耗流速。
